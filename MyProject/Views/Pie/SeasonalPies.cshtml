  @using SiddharthPieShop.ViewModel
  @model PieListViewModel
 
  
<u><h2 style="color:black ; font-family:cursive "> Pies of the Season...!</h2></u>
@*@foreach (var pie in Model)
{
    <div class="col-sm-4 col-lg-4 col-md-4">
        <div class="thumbnail">
            <img src="@pie.ImageThumbnailUrl" alt="">
            <div class="caption">
                <h3 class="pull-right">@pie.Price.ToString("c")</h3>
                <h3>
                   <td><a asp-action="Details" asp-route-id="@pie.PieId">@pie.Name</a></td>
                </h3>
                <p>@pie.ShortDescription</p>
            </div>
        </div>
    </div>
}*@
<div class="row">
     @foreach (var pie in Model.Pies)
    {
  <div class="col-sm-4">
    <div class="card">
      <div class="card-body">
          <img src="@pie.ImageThumbnailUrl" class="card-img-top" alt="...">
        <h5 class="card-title"><a asp-action="Details" asp-route-id="@pie.PieId">@pie.Name</a></h5>
        <p class="card-text">@pie.ShortDescription</p>
        <h5>Rs: @pie.Price</h5>
        <a asp-controller="pie" asp-action="AddToCart" asp-route-id="@pie.PieId"class="btn btn-primary">AddToCart</a>
      </div>
    </div>
  </div>}
 
</div>

 <li>
                <a asp-controller="Pie" asp-action="List">Home</a>
            </li>
             <li>
                <a asp-controller="Pie" asp-action="FruitsPies">Fruit Pies</a>
            </li>
             <li>
                <a asp-controller="Pie" asp-action="CheesePies">Cheese Pies</a>
            </li>
          